chain
=====

This is the skeleton project in java for the TDD Competition 2013 WordChain problem
It comprises of:
 - WordChainSolverAcceptanceTest, with 2 already passing and 1 failing acceptance tests
 - NaiveDictionaryTest, with 1 already passing unit test

To run tests:
 - for Intellij user, open chain.iml in Intellij, run Unit Tests run configuration
 - for Eclipse user
   1. File > Import > General > Existing Projects into Workspace > select unzipped top directory as root directory
   2. Run > Run History > chain